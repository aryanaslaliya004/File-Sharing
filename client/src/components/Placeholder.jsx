const Page = ({ name }) => {
    return (
        <div style={{ padding: '2rem', textAlign: 'center' }}>
            <h1>{name} Page</h1>
            <p>Coming Soon...</p>
        </div>
    );
};

export default Page;
